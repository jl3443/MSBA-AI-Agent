# 🔍 检查扩展脚本加载问题

## 问题诊断

控制台显示所有脚本都是 `undefined`，说明扩展脚本没有加载。

## 检查清单

### ✅ 1. 扩展是否启用
- `chrome://extensions/` → 确认 "UCLA MSBA AI Assistant" 开关是**蓝色**

### ✅ 2. 检查扩展错误
1. `chrome://extensions/` → 找到 "UCLA MSBA AI Assistant"
2. 查看是否有红色错误信息
3. 点击 "Errors" 按钮（如果有）
4. **告诉我错误信息是什么**

### ✅ 3. 检查脚本文件
确认这些文件都在 `msba-agent` 文件夹中：
- ✅ `manifest.json`
- ✅ `msba-knowledge-base.js`
- ✅ `msba-langchain-adapter.js`
- ✅ `msba-react-components.jsx`
- ✅ `msba-agent.js`

### ✅ 4. 检查 Content Scripts 注入
1. 打开任意网页
2. 按 `F12` 打开开发者工具
3. 点击 "Sources" 标签
4. 左侧找到 "Content scripts" → "UCLA MSBA AI Assistant"
5. 查看是否有脚本文件列表

### ✅ 5. 检查 Service Worker
1. `chrome://extensions/` → 找到 "UCLA MSBA AI Assistant"
2. 点击 "Inspect views" → "service worker"
3. 查看是否有错误

## 快速测试

在控制台运行这个，告诉我结果：

```javascript
// 检查扩展是否加载
console.log('扩展 ID:', chrome.runtime.id);
console.log('扩展名称:', chrome.runtime.getManifest().name);
console.log('脚本文件:', chrome.runtime.getManifest().content_scripts);
```

## 如果脚本还是不加载

可能的原因：
1. **文件路径错误** - 确认选择了正确的 `msba-agent` 文件夹
2. **脚本语法错误** - 检查扩展的 "Errors" 页面
3. **Content Scripts 权限问题** - 检查 manifest.json 中的 permissions

## 最可能的问题

根据你的情况，最可能是：
- **扩展脚本有语法错误**，导致加载失败
- **文件路径不正确**，Chrome 找不到脚本文件

请检查扩展的 "Errors" 页面，告诉我具体的错误信息！

